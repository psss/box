- name: Install system packages
  become: yes
  tags: system
  package:
    state: latest
    name:
    - vim
    - tree
    - python-pip
    - gnome-tweaks

- name: Install Python packages
  become: yes
  tags: system
  pip:
    state: present
    name:
    - psutil
